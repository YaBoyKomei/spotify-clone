{
  "name": "spotify-clone",
  "version": "1.0.0",
  "description": "Spotify Clone - Music Streaming App with YouTube Music Integration",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "install-all": "cd server && npm install && cd ../client && npm install && cd .."
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "music",
    "streaming",
    "spotify",
    "youtube-music",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
